//finding element not in pair in an odd sized sorted array
#include<bits/stdc++.h>

using namespace std;
int find(vector<int> arr){
    int start=0;
    int end=arr.size()-1;
    int mid=start+(end-start)/2;
    while(start<end){
        if()
    }

}
int main()
{

 return 0;
}